[x] fix workspace-wide action resolution to search all .actions files $resolution by uuid/prefix fails without -f even for the default next.actions file (bare and with --workspace both return not-found from a subdirectory cwd); only -f <path> works, so the search scope or cwd handling is broken$ !1 %2026-07-11T19:56 #019f543a-0388-7651-a388-cd3158a9c581
[x] commit and reinstall the resolution-fix \+ walker-refactor changes $clearhead-cli working tree has the lsp walker extraction (handlers.rs) and the workspace-wide verb resolution fix (commands/action.rs + tests/workspace.rs regression test), all tests green; commit then cargo install --path clearhead-cli so the daily binary gets the resolution fix$ !2 %2026-07-11T20:06 #019f5447-01aa-7792-8cac-41df16ac5c9d
[x] make --workspace filter honor config workspace_name for additional workspaces $workspace_dirs names additional workspaces via workspace_name_from_path (directory name) so --workspace only matches dir names, not the workspace's own config workspace_name; decide which should win and make it consistent$ !3 %2026-07-11T20:34 #019f5447-01bb-7fe3-a361-93fd1a8a4833
