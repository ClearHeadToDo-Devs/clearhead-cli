[ ] make --workspace filter honor config workspace_name for additional workspaces $workspace_dirs names additional workspaces via workspace_name_from_path (directory name) so --workspace only matches dir names, not the workspace's own config workspace_name; decide which should win and make it consistent$ !3 #019f5447-01bb-7fe3-a361-93fd1a8a4833
[ ] let add action default to the workspace's only charter $add action without --charter/--file errors even when the target workspace has a single obvious charter; could default to the sole charter or the workspace default next.actions$ #019f5447-01cd-70f0-a736-1907b9e13dfc
