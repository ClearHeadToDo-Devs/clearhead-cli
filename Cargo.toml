[package]
name = "clearhead_cli"
description = "A CLI tool and library for working with the clearhead framework"
version = "0.2.1"
authors = ["Darrion Burgess <darrionburgess@gmail.com>"]
edition = "2024"
license = "MIT"
repository = "https://github.com/ClearHeadToDo-Devs/clearhead_cli.git"

[dependencies]
# Core library with pure business logic
clearhead_core = { path = "../clearhead-core", version = "0.1.0" }

# CLI-specific dependencies
clap = { version = "4.0", features = ["derive"] }
comfy-table = "7.1"
config = "0.15.19"
dashmap = "6.1.0"
dirs = "6.0.0"
icalendar = "0.16.17"
reqwest = { version = "0.11.27", features = ["blocking"] }
shellexpand = "3.1"
tokio = { version = "1.48.0", features = ["full"] }
tower-lsp-server = "0.23.0"
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.22", features = ["env-filter"] }

# Note: The following are re-exported from clearhead_core but kept here
# for CLI-specific features or to ensure version compatibility
automerge = "0.7.2"
autosurgeon = { version = "0.10.1", features = ["uuid"] }
chrono = { version = "0.4", features = ["serde"] }
oxigraph = "0.5.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tree-sitter = "0.26.3"
tree-sitter-actions = { path = "../tree-sitter-actions", version = "0.9.2" }

[dependencies.uuid]
version = "1.0"
# Lets you generate random UUIDs
features = [
  "serde",
  "v4",
  "v7",
]

[dev-dependencies]
assert_cmd = "2.1"
insta = { version = "1.45.1", features = ["redactions", "ron"] }
jsonschema = "0.18"
predicates = "3.0"
tempfile = "3.8"
